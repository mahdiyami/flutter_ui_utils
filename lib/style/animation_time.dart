class AnimationTimes {
  static Duration fast = const Duration(milliseconds: 300);
  static Duration med = const Duration(milliseconds: 600);
  static Duration slow = const Duration(milliseconds: 900);
  static Duration pageTransition = const Duration(milliseconds: 200);
}
